name: Demo

on:
  schedule:
    - cron: '*/1 * * * *' # Run every day at midnight UTC
  push: 
    branches: 
      - main
  pull_request:
    branches:
      - main

jobs:
  hello:
    name: Hello
    runs-on: ubuntu-20.04
    steps: 
      - name: Checkout
        uses: actions/checkout@v3
      - run: | 
         ls
         pwd
         sudo su
      - run: echo "hello"
